# GUI-Handwritten-Digits-Recognition

The GUI used pre-trained CNN model and the model was trained using MNIST handwritten digits database.

The CNN model achieved 99.16% accuracy on validation dataset. 
